// This file is now primarily for re-exporting types and potentially data.
// The main data fetching logic is in src/lib/osdr.ts (which now reads from JSON)

import type { Publication } from './publication-type';

export { type Publication } from './publication-type';
